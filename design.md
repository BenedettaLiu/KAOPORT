# Kaohsiung Port Ship Query — 行動介面設計規格

## 設計目標

本應用程式在 **9:16 直向單手操作** 情境下，讓使用者快速掌握高雄港目前的船舶動態。初版以本機示範資料呈現，資訊架構與資料型別則維持可由 API 回傳結果取代的結構。畫面遵循 iOS Human Interface Guidelines 的層級與安全區概念，同時在 Android 裝置上維持明確、易觸及的操作方式。

## 畫面清單與內容

| 畫面 | 主要內容 | 主要功能 |
|---|---|---|
| 船舶動態首頁 | 港口標題、更新時間、搜尋欄、狀態篩選籤、船舶清單 | 以船名、航次、IMO 或泊位搜尋；依靠港、離港、在港篩選；開啟船舶詳情 |
| 船舶詳細資料 | 船名、航次、IMO、船型、國籍、狀態、靠離港時間、泊位與航向資訊 | 閱讀單一船舶完整資訊；返回保留搜尋與篩選狀態 |
| 空結果狀態 | 說明文字與清除篩選按鈕 | 清除搜尋條件，回到全部船舶清單 |

## 主要使用流程

使用者開啟應用程式後，首頁直接顯示本日船舶動態與預設的「全部」狀態。使用者可在上方搜尋列鍵入船名、航次、IMO 或泊位，清單會即時縮小。接著可選擇「在港」、「靠港」或「離港」的篩選籤，僅保留對應狀態。點選任一船舶卡片後會進入詳細資料畫面；按下左上角返回按鈕即可回到保留條件的首頁。

## 佈局與互動準則

首頁上方將港口名稱、當日日期與資料更新標記置於安全區內，讓第一屏即能讀取情境。搜尋列置於拇指容易觸及的上半部，狀態篩選採橫向可捲動的膠囊按鈕，所有按鈕與卡片維持至少 44pt 的可觸控範圍。船舶卡片採兩列資訊階層：第一列為船名與狀態，第二列為航次、泊位與時間，以減少閱讀成本。詳細頁使用區塊化資訊卡，將行程、泊位與船舶資料分開呈現。

## 色彩選擇

| 用途 | 色碼 | 說明 |
|---|---|---|
| 港灣深藍（主要色） | `#0B4F71` | 標題、已選篩選、主要行動，傳達港口與可靠資訊 |
| 海洋藍（輔助色） | `#137A9B` | 圖示、資訊重點與視覺層次 |
| 霧白背景 | `#F6F9FB` | 讓卡片與深色文字保持清晰對比 |
| 航標綠 | `#167A54` | 在港與已確認的正向狀態 |
| 琥珀橙 | `#C46A13` | 靠港或需留意的動態狀態 |
| 航道紅 | `#B94545` | 離港狀態與提醒資訊 |
| 墨藍文字 | `#173042` | 長文閱讀與主要資訊 |

## 資料呈現原則

所有示範資料會清楚以「示範資料」標示，避免被誤認為即時航運資訊。未知資料以「尚未提供」呈現，而不使用虛構數值。日後串接服務時，只需由資料存取層提供同一個 `ShipRecord` 形狀，即可沿用既有搜尋、篩選與畫面元件。
